# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  assign_terraform_review:
    if:
      - {{ files | match(list=r/.*\.tf.*) | some }}
    run:
      - action: add-reviewers@v1
        args:
          reviewers: [Shadow-Test-Organization/A-Team]
