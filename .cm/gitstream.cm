# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  check:
    if: 
      - true
    run:
      - action: add-comment@v1
        args:
          comment: |
            {{ source.diff.files | map(attr="new_content") }}
