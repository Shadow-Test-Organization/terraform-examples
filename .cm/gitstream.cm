# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  assign_terraform_review:
    if:
      - {{ files | match(regex=r/.*\.tf.*/) | some }}
    run:
      - action: add-reviewers@v1
        args:
          reviewers: [shadowshot-x]
